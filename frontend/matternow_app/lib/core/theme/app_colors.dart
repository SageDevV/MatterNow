import 'package:flutter/material.dart';

/// Paleta de cores extraída do design Figma do MatterNow.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF21A9B4); // Botão "Entrar"/"Visitar"
  static const Color primaryDark = Color(0xFF1A508E); // Links
  static const Color invite = Color(0xFF6552B4); // Título "Você foi convidada"
  static const Color inviter = Color(0xFF325656); // Nome do convidador
  static const Color text = Color(0xFF404040);
  static const Color textMuted = Color(0xFF757575);
  static const Color error = Color(0xFFFF383C);
  static const Color background = Colors.white;
  static const Color border = Color(0xFF404040);
}
